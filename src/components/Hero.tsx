import './Hero.css';

const Hero = () => {
    return (
        <section id="inicio" className="hero">
            <div className="hero-content">
                <h2>Mármoles y Granitos de Calidad</h2>
                <p>Transformamos tus espacios con los mejores materiales naturales</p>
                <button className="cta-button">Ver Catálogo</button>
            </div>
        </section>
    );
};

export default Hero;